{layout $presenter->getCoreLayout()}
{block #title}Login{/block}
{block #bodyContent}

<div class="container">
    <div class="jumbotron">
        <center>
            <h1>{$meta['name']} <small>{$meta['version']}</small></h1>
        </center>
        {include $presenter->getFlashArea()}
        <h2>
            {icon unlock} {_admin.sign.in.title}
        </h2>
        {control loginForm}
    </div>
</div>
